{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "spellcheck",
            "type": "shell",
            "command": "cspell lint '**'"
        },
        {
            "label": "set up dev environment",
            "type": "shell",
            "command": "scripts/setup-dev-environment.sh",
            "options": {
                "env": {
                    "XDEBUG_MODE": "off"
                }
            }
        }
    ]
}
