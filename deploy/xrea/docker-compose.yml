services:
  xrea:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    depends_on:
      - db

  db:
    image: mariadb:10.5
    env_file:
      - .env.mysql
    volumes:
      - xrea-mysql:/var/lib/mysql

volumes:
  xrea-mysql:
